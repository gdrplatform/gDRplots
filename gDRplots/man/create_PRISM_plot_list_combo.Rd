% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-prism-plotlist.R
\name{create_PRISM_plot_list_combo}
\alias{create_PRISM_plot_list_combo}
\title{Create a nested list of plots for PRISM data with combo metrics}
\usage{
create_PRISM_plot_list_combo(
  drug1_name_vec,
  drug2_name_vec,
  dt_metrics,
  dt_scores,
  normalization_type_vec = "RV",
  metric = c("xc50", "x_mean", "x_max"),
  metric_scores = c("hsa_score", "bliss_score"),
  fit_source = "gDR",
  meta_data_path,
  feat_data_path,
  feature_sets,
  metadata_columns = NULL,
  clear_taxonomy_info = TRUE
)
}
\arguments{
\item{drug1_name_vec}{character vector with drug names to be plotted (identifiers \code{DrugName})}

\item{drug2_name_vec}{character vector with co-drug names to be plotted (identifiers \code{DrugName_2})}

\item{dt_metrics}{\code{data.table} representing data from the \code{Metrics} assay,
outputted by \code{gDRutils::convert_se_assay_to_dt(se, "Metrics")}
and combo \code{SummarizedExperiment}}

\item{dt_scores}{\code{data.table} representing data from the \code{scores} assay,
outputted by \code{gDRutils::convert_se_assay_to_dt(se, "scores")}
and combo \code{SummarizedExperiment}}

\item{normalization_type_vec}{character vector with normalization types to be selected
one of: "GR" ("GRvalue") or "RV" ("RelativeViability") or both}

\item{metric}{character vector with names of metric;
 chosen from: "xc50" ("GR50" or "IC50" - respectively depending on \code{normalization_type}), 
"x_max" ("GR Max" or "E Max") or "x_mean" ("GR Mean" or "RV Mean")}

\item{metric_scores}{character vector with names of combo metric;
chosen from: "hsa_score"("Bliss Excess GR" or "Bliss Excess RV" - respectively 
depending on \code{normalization_type}), "bliss_score" ("Bliss Score GR" or "Bliss Score RV")}

\item{fit_source}{string source name for metrics}

\item{meta_data_path}{string path to metadata file describing all cancer models/cell lines
which are referenced by a dataset contained within the DepMap portal. 
It is usually a file named \code{Model.csv}.}

\item{feat_data_path}{string path to the directory containing the molecular feature set file to load from DepMap.}

\item{feature_sets}{character vector containing the names of the molecular feature sets to load from DepMap.
These names should also correspond to the file names containing the feature data 
(without the extension, which is assumed to be \code{csv})}

\item{metadata_columns}{character vector with the metadata columns to load for DepMap cell lines}

\item{clear_taxonomy_info}{logical flag whether to remove taxonomy information for gene names in table 
with the molecular feature sets from DepMap.}
}
\value{
nested list of plots
}
\description{
Create a nested list of plots for PRISM data with combo metrics
}
\author{
Janina Smoła \email{janina.smola@contractors.roche.com}
}
\keyword{prism_plots}
