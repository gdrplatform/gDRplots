# Order and rename columns in associations results

Order and rename columns in associations results

## Usage

``` r
.order_assoc_result(res_dt)
```

## Arguments

- res_dt:

  `data.table` of associations results

## Value

`data.table` of associations results with reorder and rename columns
