# Assert whether number may code color in rgb

Assert whether number may code color in rgb

## Usage

``` r
assert_RGB_format(x)
```

## Arguments

- x:

  numeric vector describing rgb color

## Value

`x` invisible, if the check is not successful throws an error message.
